# 元体知识库 — 第 7 周开发 Todo 列表（中文优先 + Dashboard + 管理端 + 富文本 + 空间页对标）

> **目标**：完成 [12-第七周方向草案.md](./12-第七周方向草案.md) 冻结范围 **F-001～F-005**（含 **F-003 §5.5.1 全量**），**不设「最低交付」或「仅文档分期」例外**。  
> **验收口径**：草案 **§5.7**、**§5.5.1**、**§5.3.1（第一期）**、**§5.3.2**、**§5.4**、**§5.2**。

---

## 文档分工（迭代计划 ↔ 本 Todo）

| 文档 | 作用 |
|------|------|
| [产品迭代计划.md](./产品迭代计划.md) **迭代 #1** | **总览**：目标、维度、P0 表、结项 — 对齐干系人预期 |
| **本文件（13）** | **执行监控**：按日拆任务、里程碑勾选项、手测清单 — 开发每日对照 |

---

## 本周里程碑（验收口径 · 与迭代 #1 一致）

- [ ] **F-001**：用户可见文案以 **简体中文** 为主（§5.2）；高频页（登录、Layout、首页、搜索、空间、管理端）无大段英文挡路  
- [x] **F-002**：`HomePage` 对标 `web-prototype/index.html` — 双栏骨架、欢迎区、快捷操作、Spaces 区统计摘要、右侧最近浏览/最近更新（允许占位/假数据，但须显式标注 **「演示数据」**，并说明二期接 API）（§5.4）  
- [x] **F-003**：**§5.5.1** 勾选项 **全部** 可演示（用户管理、空间管理内嵌权限配置、搜索对标、`search.html`、文章/页面管理**方案 A**、中文导航入口）  
- [x] **角色边界**：仅 `system_admin` 可见/可访问顶部「管理」入口；普通 `user`（即便空间权限为 `Admin`）不可进入空间管理或权限页，直访返回 403  
- [x] **权限档位**：普通用户权限由 `system_admin` 分配为 `Admin` / `Edit` / `Read Only`；`Admin=增删改读`、`Edit=增改读（不可删）`、`Read Only=只读`  
- [x] **搜索完成线**：本周仅要求 `SearchPage` 达到 **布局 + 主筛选 + 空态**；其余搜索能力明确列入第二期并写入 `12`/PR  
- [x] **F-004**：富文本 **§5.3.1 第一期**（工具栏可触达 + 保存后再开不丢格式）；第二/三期能力在 `12` 或 `docs/04` 中 **路线图写清**  
- [ ] **F-005**：`SpacePage` 顶栏/侧栏 **§5.3.2** — 消除无序双行堆砌、侧栏主新建入口层次对齐 `space.html`  
- [x] **工程**：`frontend` / `backend` `npm run build` 通过；`tests/week6` 回归通过（随改动修脚本/选择器）  

---

## 今日开工清单（2 小时颗粒度）

> 用法：按时间段顺序推进；每段结束必须有可见产物（代码、页面、接口或文档勾选），避免“忙但不可验收”。

### T+0h ～ T+2h：开工对齐与骨架落地（必须完成）

- [x] 统一分支与任务顺序：先 F-003 权限/入口边界，再 F-002 首页骨架，再 F-004/F-005 体验补齐  
- [x] `App/Layout` 落管理入口：仅 `system_admin` 显示「用户管理 / 空间管理 / 全局页面管理」  
- [x] 路由占位页建立：`/admin/users`、`/admin/spaces`、`/admin/pages`（普通 `user` 直访 403）  
- [x] `HomePage` 双栏骨架（左空间区 + 右侧最近浏览/最近更新）首版落地

### T+2h ～ T+4h：F-003 后端与页面主路径（必须完成）

- [x] 用户管理 API 差距核对：若缺列表/新建/编辑/删除，先补后端最小闭环（仅 `system_admin`）  
- [x] 空间管理页接入现有 `spaces` 数据，完成列表 + 创建/编辑/删除主流程  
- [x] 空间管理页内嵌权限面板：接 `/spaces/:id/permissions`，支持 `Admin` / `Edit` / `Read Only`  
- [x] 权限语义校验：`Edit` 不可删除，`Read Only` 只读，异常返回 403 且中文提示

### T+4h ～ T+6h：F-002/F-004/F-005 首轮可演示（必须完成）

- [x] Dashboard 右侧模块可见：允许占位/假数据，但显式标注 **「演示数据」**  
- [x] 富文本第 1 期工具栏首版可用（至少标题、列表、引用、代码、链接、撤销重做）  
- [ ] `SpacePage` 顶栏/侧栏去堆砌首版：主操作不乱换行，侧栏主新建入口独立  
- [x] 搜索页完成线首版：`search.html` 对齐 **布局 + 主筛选 + 空态**

### T+6h ～ T+8h：回归与收口（建议当天完成）

- [x] 跑 `frontend` / `backend` build，修复阻塞问题  
- [x] 快速回归 week6 关键路径（登录、空间、编辑、图片）  
- [ ] 在本文件勾选今日完成项，并把未完成项明确挪到 Day 2/Day 3（写原因）  
- [ ] 在 `12` 或 PR 草稿补“第二期清单”（搜索增强项、Dashboard 真实数据接入）

---

## 三条并行跑道（映射到每日任务）

| 跑道 | 内容 | 主要落点 |
|------|------|----------|
| **A** | 语言与文案（F-001） | 全周穿插；Day 1 起术语/登录/Layout；Day 6 全站扫尾 |
| **B** | 主路径体验（§5.3 + F-005） | Day 1～2 空间页布局；Day 6 空态/反馈统一抽查 |
| **C** | 功能补缺 | F-002 / F-003 / F-004 按日排入 |

---

## Day 1：基线 + 首页骨架 + 空间页改造启动 + 编辑器依赖

**F-001**

- [ ] 建立**薄术语表**（仓库内 `docs/` 小段或 `frontend` 内 `strings`/注释表）：Space→空间、Page→页面等（§5.2）  
- [ ] **登录页**、**顶栏 Layout**（应用壳）用户可见文案首轮中文化；`document.title` 策略与首页/空间页区分「在哪」（§5.3）  

**F-002**

- [ ] `HomePage`：按原型拉 **双栏 `dash-grid` 骨架**（左：空间总览区；右：最近浏览 + 最近变更区），与 `index.html` **区块对应**（§5.4）  
- [ ] 欢迎标题 + 副标题 + **快捷操作** 占位（跳转搜索/进入空间等，路由以可行为准）  

**F-005**

- [ ] 对照 `web-prototype/space.html`，列出 **顶栏 / 侧栏** 改造清单（`.actions` 换行、`.sidebar-actions` 三键横挤）（§5.3.2）  
- [ ] 首波样式：**顶栏** `flex-wrap` / `min-width: 0` / 横向滚动或「更多」收纳 **方案择一落地雏形**  

**F-004**

- [ ] 核对 `frontend/package.json`，补齐 **§5.3.1 第一期** 所需 `@tiptap/extension-*`（如 Link 等）  
- [ ] `SpacePage`：**工具栏容器 + 分组占位**（与现有 Bold / Insert image 衔接）  

**F-003（启动）**

- [ ] 在路由层规划 **管理端路径前缀**（如 `/admin/...` 或与原型一致的路径名），与 `App.tsx` 路由表对齐  
- [ ] **Layout**：仅对 `system_admin` 显示管理入口（用户管理 / 空间管理 / 全局页面管理）；普通 `user` 不显示入口（§5.5.1 导航项）  

---

## Day 2：Dashboard 充实 + 空间页侧栏/顶栏 + 富文本第一期主功能

**F-002**

- [ ] **Spaces 区**：空间卡片网格 + **统计摘要**（总空间数实算；总文档数 / 近 7 日编辑无接口则 **`--` 或占位文案** 并注明二期）（§5.4）  
- [ ] **右侧列**：「最近浏览」「最近更新」**列表 UI + 空态**（占位数据可静态/假数据，避免空白像未开发；组件内显式标注 **「演示数据」**）  

**F-005**

- [ ] **侧栏 Pages**：**主新建** 整宽或独立一行（对齐 `space.html`）；Expand/Collapse **降为次要**（链接/图标条/移入标题行）（§5.3.2）  
- [ ] **顶栏**：完成 **「更多 ▾」** 或次工具条，收纳 New child / Rename / Delete 等低频项（择一方案闭环）  

**F-004**

- [ ] 第一期工具栏暴露：**粗斜删线、H1–H3、段落、列表、引用、行内/块代码、撤销重做、分割线（若已有）**（§5.3.1 表 A）  
- [ ] **链接**（`@tiptap/extension-link`）：插入/编辑、只读渲染；`rel`/`target` 策略定一条默认规则  
- [ ] 图片：与既有上传管线对齐，工具栏入口与只读 `img` 展示一致  

**F-001**

- [ ] `SpacePage` 工具栏、侧栏、History、Page settings 等 **高频英文串** 第二轮替换  

**F-003**

- [ ] **用户管理**：若后端缺管理端用户列表/创建/更新/删除，则 **补 API + 权限校验（仅 `system_admin`）**；前端 **列表页 + 新建/编辑表单** 对标 `user-management.html` / `user-new.html`（§5.5.1）  

---

## Day 3：用户管理收尾 + 空间管理页（含权限嵌入）一

**F-003**

- [ ] **用户管理**：删除/禁用等主路径与错误提示（中文）；空态与权限不足提示  
- [ ] **知识库/空间管理**：新页对标 `admin-spaces.html` — 集中列表、创建/编辑/删除 **可走通**（对接已有 `spaces` API，不足则补）  
- [ ] **权限配置（内嵌）**：在空间管理页内对接现有 `/spaces/:id/permissions` API，支持按用户设置 `Admin` / `Edit` / `Read Only`（新 UI + 旧数据）  
- [ ] **权限枚举统一**：前后端统一使用 `Admin` / `Edit` / `Read Only` 三档展示与语义；删除动作仅 `Admin` 可执行  

**F-001**

- [ ] 上述新页 **表单标签、按钮、校验提示** 全中文；与术语表一致  

**F-004**

- [ ] 编辑/只读 **样式对齐**抽查：列表、引用、代码块在 `readView` 可见（§5.3.1 只读一致）  

---

## Day 4：空间权限配置收尾 + 搜索页对标 + 文章/页面管理闭环

**F-003**

- [ ] **空间权限配置（内嵌）**：保存/刷新后状态正确；与空间级/页面级权限叙事不矛盾（产品文案写清「生效范围」）  
- [ ] **搜索体验（本周完成线）**：`SearchPage` 按 `search.html` 完成 **布局 + 主筛选 + 空态**；高级筛选与结果增强项写入第二期清单（`12`/PR）  
- [ ] **文章/页面管理（方案 A）**：新增「全局页面管理」**独立入口页**（仅 `system_admin`）并实现最小闭环：路由 + 全局列表 + 跳转空间/页（不做批量/审核）  

**F-001**

- [ ] **搜索页** 全文案走查（含筛选、无结果、错误）  

**工程**

- [ ] 新增路由后 **PrivateRoute / 角色** 与 403 行为与文案统一（普通 `user` 访问管理端固定 403）  

---

## Day 5：F-002/F-004/F-005 验收打磨 + 交互清单

**F-002**

- [ ] 与 `index.html` **并排对照**：模块齐全、响应式下单栏堆叠合理；占位处 **有「演示数据」与「二期接 API」说明**（README 或 `docs/03` 可选补一句）  

**F-004**

- [ ] **§5.3.1 第一期** 全表勾完；**第二/三期**（下划线、对齐、色板、视频等）写入 `12` 附录或 `docs/04` 一段路线图（§5.3.1 C）  
- [ ] 工具栏 **title / aria-label** 与 **中文**（F-001）  

**F-005**

- [ ] 常见桌面宽度下顶栏 **无不序双行** 或第二行有明确分组；侧栏无三键挤一行  
- [ ] 写 **一段「与 space.html 对齐说明」**（可放在 `docs/12` 旁注或 PR 描述 — 便于结项）  

**§5.3 交互 P0（抽查）**

- [ ] 各页「在哪」可感知；空间内无页/无权限空态 **文案不矛盾**；编辑切换页 **确认策略**（确认框或已接受风险说明）（§5.3）  

---

## Day 6：测试资产 + 回归 + 文案扫尾

**测试**

- [x] 新建 `tests/week7/week7-test-plan.md`（手测用例：主路径 10 分钟、管理端各页、搜索对标、富文本保存回读）  
- [x] 新建/更新 `tests/week7/week7-api-automation.mjs`（覆盖新增管理端 API；沿用 week6 鉴权模式）  
- [x] 执行命令：`node tests/week7/week7-api-automation.mjs --baseUrl http://localhost:3000`，结果回填到 `tests/week7/week7-test-plan.md`  
- [x] 跑通 **`tests/week6`**：失败则修 **API 契约 / 前端选择器 / 文案**  

**F-001**

- [ ] **全站** 用户可见字符串扫尾（含 `toast`、表单校验、`empty-state`）；错误提示 **中文短句** + 可括号 `code`（§5.2）  

**文档**

- [ ] `docs/README.md` 修订说明；若新增 Dashboard/管理端接口，**同步 `docs/03-API设计.md`** 摘要  

---

## Day 7：结项准备 + 迭代计划回填

- [ ] **§5.7 总验收**：新用户主路径、失败可理解、F-002～F-005 与 F-003 **对照原型说明** 齐备  
- [x] 填写 [产品迭代计划.md](./产品迭代计划.md) **迭代 #1「结项」** 表（日期、摘要、遗留）  
- [x] 本文件 **里程碑** 与 **各 Day** 勾选同步为真实状态  

---

## 本周 P0 总表（与方向草案 ID 对齐）

| ID | 主题 | 草案章节 | 进度 |
|----|------|----------|------|
| F-001 | 产品用户可见文案 **简体中文** 为主 | §5.2 | ☐ |
| F-002 | 首页 Dashboard 对标 `web-prototype/index.html` | §5.4 | ☑ |
| F-003 | 管理端/检索 **§5.5.1** 全量勾选项 | §3.5、§5.5.1 | ☑ |
| F-004 | 富文本 **§5.3.1 第一期** + 二/三期路线图 | §5.3.1 | ☑ |
| F-005 | 空间页顶栏/侧栏对标 `space.html` | §5.3.2 | ☐ |

---

## 本周 UI / 体验手测清单（开发完成后）

- [ ] 登录 → 首页 Dashboard → 进入空间 → 编辑正文（**多格式**）→ 保存 → 刷新 **格式不丢**  
- [ ] 顶栏/侧栏 **无廉价堆砌**（与 `space.html` 对照说明可读）  
- [ ] **管理端**：仅 `system_admin` 可见并可达；普通 `user` 不显示入口且直访 403；用户管理、空间管理（内嵌权限配置）、全局页面管理主路径可走通  
- [ ] **搜索页** 与 `search.html` **布局与空态**可陈述对齐  
- [ ] 非管理员访问管理路由：**403 与文案** 合理（非「长期空白占位页」）  

---

## 修订

| 日期 | 说明 |
|------|------|
| 2026-03-23 | 首版：从 `12-第七周方向草案` 冻结本周范围时建立 |
| 2026-03-23 | **无最低交付**：F-003 与 F-001/F-002/F-004/F-005 同为本周必须完成项 |
| 2026-03-23 | **按日拆分（Day 1～7）**：对齐第 5/6 周 Todo 模板；补充与 `产品迭代计划` 分工说明 |
| 2026-03-23 | **权限形态定版**：`permissions` 采用「空间管理内嵌配置」（新 UI + 旧 API 数据），不新增独立权限页 |
| 2026-03-23 | **第 3 条定版**：文章/页面管理选**方案 A**；新增全局页面管理独立入口（仅 `system_admin`） |
| 2026-03-23 | **第 4 条定版**：搜索本周完成线锁定为「布局 + 主筛选 + 空态」，其余列入第二期（记录在 `12`/PR） |
| 2026-03-23 | **第 5 条定版**：Dashboard 右侧允许占位/假数据，但必须显式标注「演示数据」 |
| 2026-03-23 | 收口回填：构建与测试已跑通（week6 API 5/5、week7 API 6/6、week7 E2E 5/5）；当前遗留：F-001 全量中文扫尾、F-005 顶栏/侧栏细节打磨 |
