# 第十一周开发方向草案

> **状态**：方向草案（非最终排期）；详细任务、UI 保真清单与手测验收以当周 **`23-第十一周开发Todo.md`** 为准。  
> **基准时间**：文档撰写日 2026-04-01；与 **[Release V1.1](./21-Release-V1.1-说明.md)** 已发布版本衔接。

---

## 0. 本周已落地（更新：2026-04-01 下午）

> 以下项已在代码库落地（登录页体验迭代），并已补齐本周 Todo/测试闭环文档。

- 登录页：左右分栏布局（左侧互动角色 + 右侧表单卡片），表单密度更紧凑。
- 内网口径：移除 Google 登录入口；无注册入口，仅提示联系管理员开通。
- 品牌露出：左侧使用 `logo1.png`（圆角、合适尺寸），避免裂图路径问题（使用 `BASE_URL`）。
- 互动细节：眼睛跟随与聚焦反馈多轮对齐（橙色“上方视线”修正；紫色账号/密码聚焦差异化“偷窥”）。
- 文案：标题改为“元体WIKI，欢迎回来！”。
- 文档：新增 `docs/23-第十一周开发Todo.md`、`tests/week11/week11-test-plan.md`（并会在本周内按执行结果回填）。

---

## 1. 本周起点（与 V1.1 对齐）

以下能力已在 **V1.1** 合入，第十一周默认在其之上迭代，除非文档另有「回滚/重做」决议。

- **账号与展示**：个人资料可编辑、`PATCH /api/v1/auth/profile`、服务端头像与 `avatarRevision`、登录更新 `lastLoginAt`。
- **体验**：`/settings`（个人资料 + 显示设置/深色模式）、Dashboard 欢迎区头像、顶栏头像。
- **数据**：迁移 `20260325103000_user_profile_fields`、`20260325120000_user_avatar_path`；部署须 `npx prisma migrate deploy`。

---

## 2. 建议方向（候选，待压缩为 P0/P1）

> 下列来自 [第十周 §2 后续候选](./20-第十周开发方向草案.md)、[迭代 #3 结项遗留](./产品迭代计划.md)、[待排期 backlog](./产品迭代计划.md) 等；**正式开工前**需评审取舍并与测试计划对齐。

| ID | 方向 | 说明 |
|----|------|------|
| W11-01 | **设置与账号验收** | 个人设置页可验收用例：空邮箱、邮箱冲突（`409` / `EMAIL_IN_USE`）、头像上传失败/格式与大小、仅改单字段保存；可选：顶栏优先展示 `displayName` 于 `username`。 |
| W11-02 | **错误与权限体验** | 敏感操作（资料、头像、鉴权失败等）走统一错误展示路径；与 [ones-api-error-contract](../.cursor/rules/ones-api-error-contract.mdc) 对照复查。 |
| W11-03 | **质量门禁** | `frontend` / `backend` 构建；`tests/week7` API 与 E2E 回归；**补全第九周遗留自动化**：`tests/week9/week9-api-automation.mjs`、`frontend/tests/e2e/week9.e2e.spec.mjs`（与 [迭代 #3 结项](./产品迭代计划.md) 一致）。 |
| W11-04 | **资料 API 回归** | 为 `PATCH /api/v1/auth/profile`、头像上传/删除补充最小 API 用例（可与 W11-03 合并排期）。 |
| W11-05 | **深色模式（可选）** | 见 [19-深色模式后续迭代备忘](./19-深色模式后续迭代备忘.md)：跟随系统、`prefers-reduced-motion`、正文与代码块可读性、图片观感等；按条拆 P1，避免一次全吃。 |
| W11-06 | **运维与文档** | 根目录 README 或运维说明中强化「迁移顺序 / `prisma migrate deploy`」；与 [18-上线前演示残留扫描与清理](./18-上线前演示残留扫描与清理.md) 联动复查。 |
| W11-07 | **安全与审计（可选）** | 资料变更审计日志（后端字段或独立表）；仅在有合规/内网审计明确要求时提升为 P0。 |
| W11-08 | **产品增强（ backlog ）** | Dashboard 模块可配置、性能优化等（迭代 #3 结项已提及）；默认不作为第十一周 P0，除非当周明确冻结。 |

---

## 3. 验收与工程约束（草案级）

- **UI**：若当周动到 Space/设置等页面，须遵守仓库内 **ones UI 保真** 规则（`sidebar` / `content-area` / `content-toolbar` 等语义与原型 class）；详见 `.cursor/rules/ones-weekly-ui-checklist.mdc`、`ones-ui-engineering.mdc`。
- **错误契约**：受保护接口的 `401` / `403` / `404` / `409` 与稳定 `code` 字段保持可预期，便于前端统一映射。

---

## 4. 与相关文档的关系

- **上一 Release**：[21-Release-V1.1-说明](./21-Release-V1.1-说明.md)
- **第十周结项记录**：[20-第十周开发方向草案](./20-第十周开发方向草案.md)
- **迭代总览**：[产品迭代计划](./产品迭代计划.md)
- **深色后续**：[19-深色模式后续迭代备忘](./19-深色模式后续迭代备忘.md)

---

## 5. 修订记录

| 日期 | 说明 |
|------|------|
| 2026-04-01 | 新建：第十一周方向草案；承接 V1.1 与第十周 §2 / 迭代遗留 / backlog 候选。 |
| 2026-04-01 | 补充：记录本周下午已落地的登录页体验迭代；补齐 week11 Todo 与测试计划入口。 |
